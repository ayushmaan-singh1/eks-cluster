apiVersion: v1
kind: Service
metadata:
  name: hello-service
spec:
  selector:
    app: hello  # Ensure this matches the labels of the pod
  ports:
  - protocol: TCP
    port: 80         # The port that will be exposed
    targetPort: 80   # The port on the pod to route traffic to
  type: NodePort
